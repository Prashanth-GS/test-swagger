type: object
properties:
  success:
    success: boolean
  error:
    error: object
    properties:
      code:
        code: int
      message:
        message: string
  data:
    name: tld-list
    type: array
    items:
      $ref: ./toplevelDomain.yml
