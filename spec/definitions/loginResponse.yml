type: object
properties:
  success:
    success: boolean
  error:
    error: object
    properties:
      code:
        code: int
      message:
        message: string
  data:
    response: object
    properties:
      accessToken:
        accessToken: string
      expiresIn:
        expiresIn: string
      role:
        role: string
      userType:
        userType: string
      userRef:
        userref: string
